const initialState = {
  data: {},
  isLoading: false,
  isErorr: false,
  message: "",
};

const auth = (state = initialState, action) => {
  switch (action.type) {
    default: {
      return state;
    }
  }
};

export default auth;
